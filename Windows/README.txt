Included in this folder are two types Windows installations. Any installation that is marked as "with Tray" will run the app in the background when you try to close the app. An icon t will still exist in the system tray.

To get notifications on Windows, you'll need to add a shortcut to the Start Menu folder.
